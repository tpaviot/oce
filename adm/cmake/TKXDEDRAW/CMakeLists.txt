project(TKXDEDRAW)

if (WIN32)
  list( APPEND TKXDEDRAW_PRECOMPILED_DEFS "-D__XDEDRAW_DLL" )
  string( REGEX REPLACE ";" " " TKXDEDRAW_PRECOMPILED_DEFS "${TKXDEDRAW_PRECOMPILED_DEFS}")
endif()

  list( APPEND TKXDEDRAW_COMPILER_DIRECTORIES "${WOK_LIB_PATH}" )
  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}/inc")
    list( APPEND TKXDEDRAW_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/inc" )
  ENDIF()
  IF(EXISTS "${CMAKE_SOURCE_DIR}/inc")
    list( APPEND TKXDEDRAW_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/inc" )
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "")
    IF(EXISTS "${BUILD_PATCH_DIR}/drv/XDEDRAW")
      list( APPEND TKXDEDRAW_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/drv/XDEDRAW" )
    ENDIF()
    IF(EXISTS "${BUILD_PATCH_DIR}/src/XDEDRAW")
      list( APPEND TKXDEDRAW_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/src/XDEDRAW" )
    ENDIF()

  ENDIF()

  IF(EXISTS "${CMAKE_SOURCE_DIR}/drv/XDEDRAW")
    list( APPEND TKXDEDRAW_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/drv/XDEDRAW" )
  ENDIF()
  IF(EXISTS "${CMAKE_SOURCE_DIR}/src/XDEDRAW")
    list( APPEND TKXDEDRAW_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/src/XDEDRAW" )
  ENDIF()

IF (WIN32)
  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "")
  ENDIF()

ELSE()
  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "")
  ENDIF()

ENDIF()

  list( APPEND TKXDEDRAW_USED_LIBS TKCDF )
  list( APPEND TKXDEDRAW_USED_LIBS TKBRep )
  list( APPEND TKXDEDRAW_USED_LIBS TKXCAF )
  list( APPEND TKXDEDRAW_USED_LIBS TKernel )
  list( APPEND TKXDEDRAW_USED_LIBS TKIGES )
  list( APPEND TKXDEDRAW_USED_LIBS TKV3d )
  list( APPEND TKXDEDRAW_USED_LIBS TKMath )
  list( APPEND TKXDEDRAW_USED_LIBS TKNIS )
  list( APPEND TKXDEDRAW_USED_LIBS TKService )
  list( APPEND TKXDEDRAW_USED_LIBS TKXSBase )
  list( APPEND TKXDEDRAW_USED_LIBS TKG2d )
  list( APPEND TKXDEDRAW_USED_LIBS TKCAF )
  list( APPEND TKXDEDRAW_USED_LIBS TKDraw )
  list( APPEND TKXDEDRAW_USED_LIBS TKTopAlgo )
  list( APPEND TKXDEDRAW_USED_LIBS TKLCAF )
  list( APPEND TKXDEDRAW_USED_LIBS TKG3d )
  list( APPEND TKXDEDRAW_USED_LIBS TKSTEPBase )
  list( APPEND TKXDEDRAW_USED_LIBS TKSTEP )
  list( APPEND TKXDEDRAW_USED_LIBS TKMesh )
  list( APPEND TKXDEDRAW_USED_LIBS TKXSDRAW )
  list( APPEND TKXDEDRAW_USED_LIBS TKXDEIGES )
  list( APPEND TKXDEDRAW_USED_LIBS TKXDESTEP )
  list( APPEND TKXDEDRAW_USED_LIBS TKDCAF )
  list( APPEND TKXDEDRAW_USED_LIBS TKViewerTest )

if (WIN32)
elseif(APPLE)
else()
endif()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW.cxx")
    list( APPEND TKXDEDRAW_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW.cxx" )
    SOURCE_GROUP (XDEDRAW FILES "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW.cxx")
      list( APPEND TKXDEDRAW_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW.cxx" )
      SOURCE_GROUP (XDEDRAW FILES "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Colors.cxx")
    list( APPEND TKXDEDRAW_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Colors.cxx" )
    SOURCE_GROUP (XDEDRAW FILES "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Colors.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Colors.cxx")
      list( APPEND TKXDEDRAW_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Colors.cxx" )
      SOURCE_GROUP (XDEDRAW FILES "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Colors.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Common.cxx")
    list( APPEND TKXDEDRAW_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Common.cxx" )
    SOURCE_GROUP (XDEDRAW FILES "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Common.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Common.cxx")
      list( APPEND TKXDEDRAW_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Common.cxx" )
      SOURCE_GROUP (XDEDRAW FILES "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Common.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Layers.cxx")
    list( APPEND TKXDEDRAW_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Layers.cxx" )
    SOURCE_GROUP (XDEDRAW FILES "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Layers.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Layers.cxx")
      list( APPEND TKXDEDRAW_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Layers.cxx" )
      SOURCE_GROUP (XDEDRAW FILES "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Layers.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Props.cxx")
    list( APPEND TKXDEDRAW_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Props.cxx" )
    SOURCE_GROUP (XDEDRAW FILES "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Props.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Props.cxx")
      list( APPEND TKXDEDRAW_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Props.cxx" )
      SOURCE_GROUP (XDEDRAW FILES "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Props.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Shapes.cxx")
    list( APPEND TKXDEDRAW_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Shapes.cxx" )
    SOURCE_GROUP (XDEDRAW FILES "${BUILD_PATCH_DIR}//src/XDEDRAW/XDEDRAW_Shapes.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Shapes.cxx")
      list( APPEND TKXDEDRAW_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Shapes.cxx" )
      SOURCE_GROUP (XDEDRAW FILES "${CMAKE_SOURCE_DIR}//src/XDEDRAW/XDEDRAW_Shapes.cxx")
    ENDIF()
  ENDIF()


IF (WIN32)
ELSE()

  IF (APPLE)

  ENDIF()

ENDIF()

if ("${USED_TOOLKITS}" STREQUAL "" OR DEFINED TurnONtheTKXDEDRAW)
 add_library( TKXDEDRAW SHARED ${TKXDEDRAW_USED_SRCFILES} )

 set_property(TARGET TKXDEDRAW PROPERTY FOLDER Draw)

 install( TARGETS TKXDEDRAW
                                 RUNTIME DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/bin${BUILD_SUFFIX}"
                                 ARCHIVE DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/lib${BUILD_SUFFIX}"
                                 LIBRARY DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/lib${BUILD_SUFFIX}")

 if (MSVC)
  install( FILES  ${CMAKE_BINARY_DIR}/out/bin/Debug/TKXDEDRAW.pdb CONFIGURATIONS Debug
                                  DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/bin${BUILD_SUFFIX}")
 endif()


 set_target_properties( TKXDEDRAW PROPERTIES COMPILE_FLAGS "${TKXDEDRAW_PRECOMPILED_DEFS}" )
 include_directories( ${TKXDEDRAW_COMPILER_DIRECTORIES} )
 target_link_libraries( TKXDEDRAW ${TKXDEDRAW_USED_LIBS} )
endif()
