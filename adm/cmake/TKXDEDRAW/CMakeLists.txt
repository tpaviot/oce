project(TKXDEDRAW)

if (WIN32)
  list( APPEND TKXDEDRAW_PRECOMPILED_DEFS "-D__XDEDRAW_DLL" )
  string( REGEX REPLACE ";" " " TKXDEDRAW_PRECOMPILED_DEFS "${TKXDEDRAW_PRECOMPILED_DEFS}")
endif()

  list( APPEND TKXDEDRAW_COMPILER_DIRECTORIES "${WOK_LIB_PATH}" )
  list( APPEND TKXDEDRAW_COMPILER_DIRECTORIES "../../../inc" )
  list( APPEND TKXDEDRAW_COMPILER_DIRECTORIES "../../../drv/XDEDRAW" )
  list( APPEND TKXDEDRAW_COMPILER_DIRECTORIES "../../../src/XDEDRAW" )

if (WIN32)
else()
endif()

  list( APPEND TKXDEDRAW_USED_LIBS TKCDF )
  list( APPEND TKXDEDRAW_USED_LIBS TKBRep )
  list( APPEND TKXDEDRAW_USED_LIBS TKXCAF )
  list( APPEND TKXDEDRAW_USED_LIBS TKernel )
  list( APPEND TKXDEDRAW_USED_LIBS TKIGES )
  list( APPEND TKXDEDRAW_USED_LIBS TKV3d )
  list( APPEND TKXDEDRAW_USED_LIBS TKMath )
  list( APPEND TKXDEDRAW_USED_LIBS TKNIS )
  list( APPEND TKXDEDRAW_USED_LIBS TKService )
  list( APPEND TKXDEDRAW_USED_LIBS TKXSBase )
  list( APPEND TKXDEDRAW_USED_LIBS TKG2d )
  list( APPEND TKXDEDRAW_USED_LIBS TKCAF )
  list( APPEND TKXDEDRAW_USED_LIBS TKDraw )
  list( APPEND TKXDEDRAW_USED_LIBS TKTopAlgo )
  list( APPEND TKXDEDRAW_USED_LIBS TKLCAF )
  list( APPEND TKXDEDRAW_USED_LIBS TKG3d )
  list( APPEND TKXDEDRAW_USED_LIBS TKSTEPBase )
  list( APPEND TKXDEDRAW_USED_LIBS TKSTEP )
  list( APPEND TKXDEDRAW_USED_LIBS TKMesh )
  list( APPEND TKXDEDRAW_USED_LIBS TKXSDRAW )
  list( APPEND TKXDEDRAW_USED_LIBS TKXDEIGES )
  list( APPEND TKXDEDRAW_USED_LIBS TKXDESTEP )
  list( APPEND TKXDEDRAW_USED_LIBS TKDCAF )
  list( APPEND TKXDEDRAW_USED_LIBS TKViewerTest )

if (WIN32)
elseif(APPLE)
else()
endif()

  list( APPEND TKXDEDRAW_USED_SRCFILES "../../../src/XDEDRAW/XDEDRAW.cxx" )
  SOURCE_GROUP (XDEDRAW FILES "../../../src/XDEDRAW/XDEDRAW.cxx")

  list( APPEND TKXDEDRAW_USED_SRCFILES "../../../src/XDEDRAW/XDEDRAW_Colors.cxx" )
  SOURCE_GROUP (XDEDRAW FILES "../../../src/XDEDRAW/XDEDRAW_Colors.cxx")

  list( APPEND TKXDEDRAW_USED_SRCFILES "../../../src/XDEDRAW/XDEDRAW_Common.cxx" )
  SOURCE_GROUP (XDEDRAW FILES "../../../src/XDEDRAW/XDEDRAW_Common.cxx")

  list( APPEND TKXDEDRAW_USED_SRCFILES "../../../src/XDEDRAW/XDEDRAW_Layers.cxx" )
  SOURCE_GROUP (XDEDRAW FILES "../../../src/XDEDRAW/XDEDRAW_Layers.cxx")

  list( APPEND TKXDEDRAW_USED_SRCFILES "../../../src/XDEDRAW/XDEDRAW_Props.cxx" )
  SOURCE_GROUP (XDEDRAW FILES "../../../src/XDEDRAW/XDEDRAW_Props.cxx")

  list( APPEND TKXDEDRAW_USED_SRCFILES "../../../src/XDEDRAW/XDEDRAW_Shapes.cxx" )
  SOURCE_GROUP (XDEDRAW FILES "../../../src/XDEDRAW/XDEDRAW_Shapes.cxx")

if (WIN32)
else()

  if (APPLE)

  endif()

endif()

if ("${USED_TOOLKITS}" STREQUAL "" OR DEFINED TurnONtheTKXDEDRAW)
 add_library( TKXDEDRAW SHARED ${TKXDEDRAW_USED_SRCFILES} )

 set_property(TARGET TKXDEDRAW PROPERTY FOLDER Draw)

 install( TARGETS TKXDEDRAW
                                 RUNTIME DESTINATION "${INSTALL_DIR}/bin"
                                 ARCHIVE DESTINATION "${INSTALL_DIR}/lib"
                                 LIBRARY DESTINATION "${INSTALL_DIR}/lib")

 if (MSVC)
  install( FILES  ${CMAKE_BINARY_DIR}/out/bin/Debug/TKXDEDRAW.pdb CONFIGURATIONS Debug
                                  DESTINATION "${INSTALL_DIR}/bin")
 endif()


 set_target_properties( TKXDEDRAW PROPERTIES COMPILE_FLAGS "${TKXDEDRAW_PRECOMPILED_DEFS}" )
 include_directories( ${TKXDEDRAW_COMPILER_DIRECTORIES} )
 target_link_libraries( TKXDEDRAW ${TKXDEDRAW_USED_LIBS} )
endif()
