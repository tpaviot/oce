SET(TOOLKIT TKMesh)

SET(TOOLKIT_MODULES
	MeshDS
	BRepMesh
	MeshAlgo
	IntPoly
)
SET(TOOLKIT_DEPENDS
	TKMath
	TKernel
	TKG2d
	TKGeomBase
	TKBRep
	TKG3d
	TKGeomAlgo
	TKTopAlgo
)
IF(TBB_LIBRARY AND ${PROJECT_NAME}_WITH_TBB)
	ADD_DEFINITIONS(-DHAVE_TBB)
	SET(TOOLKIT_INCLUDE_DIRECTORIES ${TBB_INCLUDE_DIR})
	SET(TOOLKIT_LIBS ${TBB_LIBRARY})
ELSE(TBB_LIBRARY AND ${PROJECT_NAME}_WITH_TBB)
	IF(OPENMP_FOUND AND ${PROJECT_NAME}_WITH_OPENMP)
		SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${OpenMP_CXX_FLAGS}")
	ENDIF(OPENMP_FOUND AND ${PROJECT_NAME}_WITH_OPENMP)
ENDIF(TBB_LIBRARY AND ${PROJECT_NAME}_WITH_TBB)

INCLUDE(../OccBuildKit.cmake)

