project(TKTObjDRAW)

if (WIN32)
  list( APPEND TKTObjDRAW_PRECOMPILED_DEFS "-D__TObjDRAW_DLL" )
  string( REGEX REPLACE ";" " " TKTObjDRAW_PRECOMPILED_DEFS "${TKTObjDRAW_PRECOMPILED_DEFS}")
endif()

  list( APPEND TKTObjDRAW_COMPILER_DIRECTORIES "${WOK_LIB_PATH}" )
  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}/inc")
    list( APPEND TKTObjDRAW_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/inc" )
  ENDIF()
  IF(EXISTS "${CMAKE_SOURCE_DIR}/inc")
    list( APPEND TKTObjDRAW_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/inc" )
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "")
    IF(EXISTS "${BUILD_PATCH_DIR}/drv/TObjDRAW")
      list( APPEND TKTObjDRAW_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/drv/TObjDRAW" )
    ENDIF()
    IF(EXISTS "${BUILD_PATCH_DIR}/src/TObjDRAW")
      list( APPEND TKTObjDRAW_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/src/TObjDRAW" )
    ENDIF()

  ENDIF()

  IF(EXISTS "${CMAKE_SOURCE_DIR}/drv/TObjDRAW")
    list( APPEND TKTObjDRAW_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/drv/TObjDRAW" )
  ENDIF()
  IF(EXISTS "${CMAKE_SOURCE_DIR}/src/TObjDRAW")
    list( APPEND TKTObjDRAW_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/src/TObjDRAW" )
  ENDIF()

IF (WIN32)
  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "")
  ENDIF()

ELSE()
  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "")
  ENDIF()

ENDIF()

  list( APPEND TKTObjDRAW_USED_LIBS TKernel )
  list( APPEND TKTObjDRAW_USED_LIBS TKCDF )
  list( APPEND TKTObjDRAW_USED_LIBS TKLCAF )
  list( APPEND TKTObjDRAW_USED_LIBS TKTObj )
  list( APPEND TKTObjDRAW_USED_LIBS TKMath )
  list( APPEND TKTObjDRAW_USED_LIBS TKDraw )
  list( APPEND TKTObjDRAW_USED_LIBS TKDCAF )

if (WIN32)
elseif(APPLE)
elseif(ANDROID)
else()
endif()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/TObjDRAW/TObjDRAW.cxx")
    list( APPEND TKTObjDRAW_USED_SRCFILES "${BUILD_PATCH_DIR}//src/TObjDRAW/TObjDRAW.cxx" )
    SOURCE_GROUP (TObjDRAW FILES "${BUILD_PATCH_DIR}//src/TObjDRAW/TObjDRAW.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/TObjDRAW/TObjDRAW.cxx")
      list( APPEND TKTObjDRAW_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/TObjDRAW/TObjDRAW.cxx" )
      SOURCE_GROUP (TObjDRAW FILES "${CMAKE_SOURCE_DIR}//src/TObjDRAW/TObjDRAW.cxx")
    ENDIF()
  ENDIF()


IF (WIN32)
ELSE()

  IF (APPLE)

  ENDIF()

ENDIF()

if ("${USED_TOOLKITS}" STREQUAL "" OR DEFINED TurnONtheTKTObjDRAW)
 add_library( TKTObjDRAW SHARED ${TKTObjDRAW_USED_SRCFILES} )

 set_property(TARGET TKTObjDRAW PROPERTY FOLDER Draw)

 install( TARGETS TKTObjDRAW
                                 RUNTIME DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/bin${BUILD_POSTFIX}"
                                 ARCHIVE DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/lib${BUILD_POSTFIX}"
                                 LIBRARY DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/lib${BUILD_POSTFIX}")

 if (MSVC)
  install( FILES  ${CMAKE_BINARY_DIR}/out/bind/Debug/TKTObjDRAW.pdb CONFIGURATIONS Debug
                                  DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/bin${BUILD_POSTFIX}")
 endif()


 set_target_properties( TKTObjDRAW PROPERTIES COMPILE_FLAGS "${TKTObjDRAW_PRECOMPILED_DEFS}" )
 include_directories( ${TKTObjDRAW_COMPILER_DIRECTORIES} )
 target_link_libraries( TKTObjDRAW ${TKTObjDRAW_USED_LIBS} )
endif()
