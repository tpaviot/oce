project(TKXmlXCAF)

if (WIN32)
  list( APPEND TKXmlXCAF_PRECOMPILED_DEFS "-D__XmlXCAFDrivers_DLL" )
  list( APPEND TKXmlXCAF_PRECOMPILED_DEFS "-D__XmlMXCAFDoc_DLL" )
  string( REGEX REPLACE ";" " " TKXmlXCAF_PRECOMPILED_DEFS "${TKXmlXCAF_PRECOMPILED_DEFS}")
endif()

  list( APPEND TKXmlXCAF_COMPILER_DIRECTORIES "${WOK_LIB_PATH}" )
  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}/inc")
    list( APPEND TKXmlXCAF_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/inc" )
  ENDIF()
  IF(EXISTS "${CMAKE_SOURCE_DIR}/inc")
    list( APPEND TKXmlXCAF_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/inc" )
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "")
    IF(EXISTS "${BUILD_PATCH_DIR}/drv/XmlXCAFDrivers")
      list( APPEND TKXmlXCAF_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/drv/XmlXCAFDrivers" )
    ENDIF()
    IF(EXISTS "${BUILD_PATCH_DIR}/src/XmlXCAFDrivers")
      list( APPEND TKXmlXCAF_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/src/XmlXCAFDrivers" )
    ENDIF()

    IF(EXISTS "${BUILD_PATCH_DIR}/drv/XmlMXCAFDoc")
      list( APPEND TKXmlXCAF_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/drv/XmlMXCAFDoc" )
    ENDIF()
    IF(EXISTS "${BUILD_PATCH_DIR}/src/XmlMXCAFDoc")
      list( APPEND TKXmlXCAF_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/src/XmlMXCAFDoc" )
    ENDIF()

  ENDIF()

  IF(EXISTS "${CMAKE_SOURCE_DIR}/drv/XmlXCAFDrivers")
    list( APPEND TKXmlXCAF_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/drv/XmlXCAFDrivers" )
  ENDIF()
  IF(EXISTS "${CMAKE_SOURCE_DIR}/src/XmlXCAFDrivers")
    list( APPEND TKXmlXCAF_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/src/XmlXCAFDrivers" )
  ENDIF()

  IF(EXISTS "${CMAKE_SOURCE_DIR}/drv/XmlMXCAFDoc")
    list( APPEND TKXmlXCAF_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/drv/XmlMXCAFDoc" )
  ENDIF()
  IF(EXISTS "${CMAKE_SOURCE_DIR}/src/XmlMXCAFDoc")
    list( APPEND TKXmlXCAF_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/src/XmlMXCAFDoc" )
  ENDIF()

IF (WIN32)
  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "")
  ENDIF()

ELSE()
  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "")
  ENDIF()

ENDIF()

  list( APPEND TKXmlXCAF_USED_LIBS TKXmlL )
  list( APPEND TKXmlXCAF_USED_LIBS TKBRep )
  list( APPEND TKXmlXCAF_USED_LIBS TKCDF )
  list( APPEND TKXmlXCAF_USED_LIBS TKMath )
  list( APPEND TKXmlXCAF_USED_LIBS TKernel )
  list( APPEND TKXmlXCAF_USED_LIBS TKG2d )
  list( APPEND TKXmlXCAF_USED_LIBS TKGeomBase )
  list( APPEND TKXmlXCAF_USED_LIBS TKCAF )
  list( APPEND TKXmlXCAF_USED_LIBS TKG3d )
  list( APPEND TKXmlXCAF_USED_LIBS TKLCAF )
  list( APPEND TKXmlXCAF_USED_LIBS TKXCAF )
  list( APPEND TKXmlXCAF_USED_LIBS TKXml )

if (WIN32)
elseif(APPLE)
elseif(ANDROID)
else()
endif()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers.cxx" )
    SOURCE_GROUP (XmlXCAFDrivers FILES "${BUILD_PATCH_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers.cxx" )
      SOURCE_GROUP (XmlXCAFDrivers FILES "${CMAKE_SOURCE_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers_DocumentRetrievalDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers_DocumentRetrievalDriver.cxx" )
    SOURCE_GROUP (XmlXCAFDrivers FILES "${BUILD_PATCH_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers_DocumentRetrievalDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers_DocumentRetrievalDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers_DocumentRetrievalDriver.cxx" )
      SOURCE_GROUP (XmlXCAFDrivers FILES "${CMAKE_SOURCE_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers_DocumentRetrievalDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers_DocumentStorageDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers_DocumentStorageDriver.cxx" )
    SOURCE_GROUP (XmlXCAFDrivers FILES "${BUILD_PATCH_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers_DocumentStorageDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers_DocumentStorageDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers_DocumentStorageDriver.cxx" )
      SOURCE_GROUP (XmlXCAFDrivers FILES "${CMAKE_SOURCE_DIR}//src/XmlXCAFDrivers/XmlXCAFDrivers_DocumentStorageDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_AreaDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_AreaDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_AreaDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_AreaDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_AreaDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_AreaDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_CentroidDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_CentroidDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_CentroidDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_CentroidDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_CentroidDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_CentroidDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ColorDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ColorDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ColorDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ColorDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ColorDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ColorDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ColorToolDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ColorToolDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ColorToolDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ColorToolDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ColorToolDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ColorToolDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DatumDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DatumDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DatumDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DatumDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DatumDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DatumDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DimTolDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DimTolDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DimTolDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DimTolDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DimTolDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DimTolDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DimTolToolDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DimTolToolDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DimTolToolDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DimTolToolDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DimTolToolDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DimTolToolDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DocumentToolDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DocumentToolDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DocumentToolDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DocumentToolDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DocumentToolDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_DocumentToolDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_GraphNodeDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_GraphNodeDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_GraphNodeDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_GraphNodeDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_GraphNodeDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_GraphNodeDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_LayerToolDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_LayerToolDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_LayerToolDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_LayerToolDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_LayerToolDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_LayerToolDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_LocationDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_LocationDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_LocationDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_LocationDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_LocationDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_LocationDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_MaterialDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_MaterialDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_MaterialDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_MaterialDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_MaterialDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_MaterialDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_MaterialToolDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_MaterialToolDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_MaterialToolDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_MaterialToolDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_MaterialToolDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_MaterialToolDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ShapeToolDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ShapeToolDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ShapeToolDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ShapeToolDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ShapeToolDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_ShapeToolDriver.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_VolumeDriver.cxx")
    list( APPEND TKXmlXCAF_USED_SRCFILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_VolumeDriver.cxx" )
    SOURCE_GROUP (XmlMXCAFDoc FILES "${BUILD_PATCH_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_VolumeDriver.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_VolumeDriver.cxx")
      list( APPEND TKXmlXCAF_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_VolumeDriver.cxx" )
      SOURCE_GROUP (XmlMXCAFDoc FILES "${CMAKE_SOURCE_DIR}//src/XmlMXCAFDoc/XmlMXCAFDoc_VolumeDriver.cxx")
    ENDIF()
  ENDIF()


IF (WIN32)
ELSE()

  IF (APPLE)

  ENDIF()

ENDIF()

if ("${USED_TOOLKITS}" STREQUAL "" OR DEFINED TurnONtheTKXmlXCAF)
 add_library( TKXmlXCAF SHARED ${TKXmlXCAF_USED_SRCFILES} )

 set_property(TARGET TKXmlXCAF PROPERTY FOLDER DataExchange)

 install( TARGETS TKXmlXCAF
                                 RUNTIME DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/bin${BUILD_POSTFIX}"
                                 ARCHIVE DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/lib${BUILD_POSTFIX}"
                                 LIBRARY DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/lib${BUILD_POSTFIX}")

 if (MSVC)
  install( FILES  ${CMAKE_BINARY_DIR}/out/bind/Debug/TKXmlXCAF.pdb CONFIGURATIONS Debug
                                  DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/bin${BUILD_POSTFIX}")
 endif()


 set_target_properties( TKXmlXCAF PROPERTIES COMPILE_FLAGS "${TKXmlXCAF_PRECOMPILED_DEFS}" )
 include_directories( ${TKXmlXCAF_COMPILER_DIRECTORIES} )
 target_link_libraries( TKXmlXCAF ${TKXmlXCAF_USED_LIBS} )
endif()
