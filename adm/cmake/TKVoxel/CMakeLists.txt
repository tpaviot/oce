project(TKVoxel)

if (WIN32)
  list( APPEND TKVoxel_PRECOMPILED_DEFS "-D__Voxel_DLL" )
  string( REGEX REPLACE ";" " " TKVoxel_PRECOMPILED_DEFS "${TKVoxel_PRECOMPILED_DEFS}")
endif()

  list( APPEND TKVoxel_COMPILER_DIRECTORIES "${WOK_LIB_PATH}" )
  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}/inc")
    list( APPEND TKVoxel_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/inc" )
  ENDIF()
  IF(EXISTS "${CMAKE_SOURCE_DIR}/inc")
    list( APPEND TKVoxel_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/inc" )
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "")
    IF(EXISTS "${BUILD_PATCH_DIR}/drv/Voxel")
      list( APPEND TKVoxel_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/drv/Voxel" )
    ENDIF()
    IF(EXISTS "${BUILD_PATCH_DIR}/src/Voxel")
      list( APPEND TKVoxel_COMPILER_DIRECTORIES "${BUILD_PATCH_DIR}/src/Voxel" )
    ENDIF()

  ENDIF()

  IF(EXISTS "${CMAKE_SOURCE_DIR}/drv/Voxel")
    list( APPEND TKVoxel_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/drv/Voxel" )
  ENDIF()
  IF(EXISTS "${CMAKE_SOURCE_DIR}/src/Voxel")
    list( APPEND TKVoxel_COMPILER_DIRECTORIES "${CMAKE_SOURCE_DIR}/src/Voxel" )
  ENDIF()

IF (WIN32)
  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "")
  ENDIF()

ELSE()
  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "")
  ENDIF()

ENDIF()

  list( APPEND TKVoxel_USED_LIBS TKBRep )
  list( APPEND TKVoxel_USED_LIBS TKernel )
  list( APPEND TKVoxel_USED_LIBS TKV3d )
  list( APPEND TKVoxel_USED_LIBS TKMath )
  list( APPEND TKVoxel_USED_LIBS TKService )
  list( APPEND TKVoxel_USED_LIBS TKGeomBase )
  list( APPEND TKVoxel_USED_LIBS TKG2d )
  list( APPEND TKVoxel_USED_LIBS TKTopAlgo )
  list( APPEND TKVoxel_USED_LIBS TKG3d )
  list( APPEND TKVoxel_USED_LIBS TKMesh )

if (WIN32)
elseif(APPLE)
else()
endif()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_BoolDS.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_BoolDS.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_BoolDS.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_BoolDS.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_BoolDS.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_BoolDS.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_BooleanOperation.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_BooleanOperation.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_BooleanOperation.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_BooleanOperation.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_BooleanOperation.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_BooleanOperation.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_CollisionDetection.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_CollisionDetection.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_CollisionDetection.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_CollisionDetection.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_CollisionDetection.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_CollisionDetection.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_ColorDS.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_ColorDS.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_ColorDS.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_ColorDS.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_ColorDS.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_ColorDS.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_DS.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_DS.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_DS.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_DS.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_DS.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_DS.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_FastConverter.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_FastConverter.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_FastConverter.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_FastConverter.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_FastConverter.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_FastConverter.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_FloatDS.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_FloatDS.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_FloatDS.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_FloatDS.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_FloatDS.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_FloatDS.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_OctBoolDS.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_OctBoolDS.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_OctBoolDS.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_OctBoolDS.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_OctBoolDS.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_OctBoolDS.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_Prs.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_Prs.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_Prs.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_Prs.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_Prs.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_Prs.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_ROctBoolDS.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_ROctBoolDS.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_ROctBoolDS.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_ROctBoolDS.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_ROctBoolDS.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_ROctBoolDS.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_Reader.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_Reader.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_Reader.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_Reader.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_Reader.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_Reader.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_Selector.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_Selector.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_Selector.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_Selector.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_Selector.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_Selector.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_SplitData.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_SplitData.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_SplitData.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_SplitData.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_SplitData.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_SplitData.cxx")
    ENDIF()
  ENDIF()

  IF(NOT "${BUILD_PATCH_DIR}" STREQUAL "" AND EXISTS "${BUILD_PATCH_DIR}//src/Voxel/Voxel_Writer.cxx")
    list( APPEND TKVoxel_USED_SRCFILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_Writer.cxx" )
    SOURCE_GROUP (Voxel FILES "${BUILD_PATCH_DIR}//src/Voxel/Voxel_Writer.cxx")
  ELSE()
    IF(EXISTS "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_Writer.cxx")
      list( APPEND TKVoxel_USED_SRCFILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_Writer.cxx" )
      SOURCE_GROUP (Voxel FILES "${CMAKE_SOURCE_DIR}//src/Voxel/Voxel_Writer.cxx")
    ENDIF()
  ENDIF()


IF (WIN32)
ELSE()

  IF (APPLE)

  ENDIF()

ENDIF()

if ("${USED_TOOLKITS}" STREQUAL "" OR DEFINED TurnONtheTKVoxel)
 add_library( TKVoxel SHARED ${TKVoxel_USED_SRCFILES} )

 set_property(TARGET TKVoxel PROPERTY FOLDER Visualization)

 install( TARGETS TKVoxel
                                 RUNTIME DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/bin${BUILD_SUFFIX}"
                                 ARCHIVE DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/lib${BUILD_SUFFIX}"
                                 LIBRARY DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/lib${BUILD_SUFFIX}")

 if (MSVC)
  install( FILES  ${CMAKE_BINARY_DIR}/out/bin/Debug/TKVoxel.pdb CONFIGURATIONS Debug
                                  DESTINATION "${INSTALL_DIR}/${OS_WITH_BIT}/${COMPILER}/bin${BUILD_SUFFIX}")
 endif()


 set_target_properties( TKVoxel PROPERTIES COMPILE_FLAGS "${TKVoxel_PRECOMPILED_DEFS}" )
 include_directories( ${TKVoxel_COMPILER_DIRECTORIES} )
 target_link_libraries( TKVoxel ${TKVoxel_USED_LIBS} )
endif()
