set(TOOLKIT TKViewerTest)

set(TOOLKIT_MODULES
	ViewerTest
)
set(TOOLKIT_DEPENDS
	TKGeomBase
	TKFillet
	TKBRep
	TKTopAlgo
	TKHLR
	TKernel
	TKMath
	TKNIS
	TKService
	TKBool
	TKPrim
	TKGeomAlgo
	TKG2d
	TKTopTest
	TKG3d
	TKOffset
	TKVoxel
	TKMesh
	TKV3d
	TKDraw
	TKOpenGl
)

if(WIN32)
	set(TOOLKIT_LIBS ${CSF_user32_LIB} ${CSF_gdi32_LIB})
else(WIN32)
	if (OCE_OSX_USE_COCOA)
		set(TOOLKIT_INCLUDE_DIRECTORIES ${TK_INCLUDE_PATH} ${OPENGL_INCLUDE_DIR})
		set(TOOLKIT_LIBS ${TK_LIBRARY})
	else(OCE_OSX_USE_COCOA)
		set(TOOLKIT_INCLUDE_DIRECTORIES ${X11_X11_INCLUDE_PATH} ${TK_INCLUDE_PATH} ${OPENGL_INCLUDE_DIR})
		set(TOOLKIT_LIBS ${TK_LIBRARY} ${X11_X11_LIB})
	endif(OCE_OSX_USE_COCOA)
endif(WIN32)

set(TOOLKIT_IS_PRIVATE TRUE)

include(../BuildToolkit.cmake)

